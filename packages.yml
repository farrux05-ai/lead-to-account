# ============================================================
# dbt Package Dependencies
# ============================================================
# dbt_utils  : Cross-db macros (generate_surrogate_key,
#              date_spine, safe_divide, etc.)
# ============================================================

packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
  - package: calogica/dbt_expectations
    version: [">=0.10.0", "<0.11.0"]
