version: 2

models:
  - name: stg_ga4__sessions_daily
    description: "Pre-aggregated daily session data from Google Analytics 4."
    columns:
      - name: metric_date
        description: "Date of activity."
      - name: utm_source
        description: "Marketing source (e.g. google, linkedin)."


